
dependencies:
  override:
    - make
    - make ci_install

  cache_directories:
    - ".colab-wheel/"
    - "solr-4.10.3.tgz"

database:
  post:
    - make run_solr:
        background: true

test:
  override:
    - make test
